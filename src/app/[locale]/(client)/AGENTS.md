# Client sector

Before editing any file under `(client)/**`:

1. Invoke `/client-agent` skill
2. Read `.agents/client-agent/SECTOR.md`

After changes: update that SECTOR.md with decisions, components touched, and cross-sector impacts.

Full feature vision (user's raw words): `.knowledge/vision.md`. Design rules and anti-patterns: `.agents/client-agent/SKILL.md`.
