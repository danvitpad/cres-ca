/** --- YAML
 * name: MarketingLayout
 * description: Pass-through layout for marketing section — navigation is handled by main sidebar flyout
 * --- */

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
