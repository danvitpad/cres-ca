/** --- YAML
 * name: CatalogueLayout
 * description: Pass-through layout for catalogue section — navigation is handled by main sidebar flyout
 * --- */

export default function CatalogueLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
