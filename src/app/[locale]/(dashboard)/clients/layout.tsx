/** --- YAML
 * name: ClientsLayout
 * description: Pass-through layout for clients section — navigation is handled by main sidebar flyout
 * --- */

export default function ClientsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
