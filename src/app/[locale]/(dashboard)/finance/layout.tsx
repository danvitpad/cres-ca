/** --- YAML
 * name: FinanceLayout
 * description: Pass-through layout for finance section — navigation is handled by main sidebar flyout
 * --- */

export default function FinanceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
